---
type Props = {
    position?: "start" | "center" | "end";
};

const { position = "start" } = Astro.props;

const positionClass = `text-${position}`;
---

<section class:list={["empty:hidden", positionClass]}>
    <slot />
</section>

<style>
    section :global(h2) {
        @apply font-merriweather text-coral leading-6 text-center text-xl md:text-2xl lg:text-3xl font-light md:font-normal;
    }

    section :global(h3) {
        @apply font-merriweather text-lg font-semibold;
    }

    section :global(p) {
        @apply text-base font-merriweather font-light leading-[1.24] no-underline;
    }

    section :global(#business-content),
    section :global(#article-content) {
        @apply text-coral;
    }
</style>
