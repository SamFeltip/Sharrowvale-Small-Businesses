---
type Props = {
    classList?: string;
    color?: "white" | "coral" | "black";
    id?: string;
    position?: "start" | "center" | "end";
};

const props: Props = Astro.props;

// Assign default value for props
export const defaultProps: Props = {
    color: "white",
    position: "start",
};
---

<h2
    id={props.id}
    class:list={[
        "font-merriweather text-4xl font-normal capitalize",
        `text-${props.color}`,
        `text-${props.position}`,
        props.classList,
    ]}
>
    <slot />
</h2>
