---
type Props = {
    position?: "start" | "center" | "end";
};

const { position = "start" } = Astro.props;

const positionClass = `text-${position}`;
---

<section class="pb-4 md:pb-5" class:list={["empty:hidden", positionClass]}>
    <slot />
</section>

<style>
    section :global(h2) {
        @apply font-merriweather text-coral capitalize text-2xl md:text-3xl lg:text-4xl font-normal mt-4 mb-1;
    }

    section :global(h3) {
        @apply font-merriweather text-lg mb-2;
    }

    section :global(p),
    section :global(td) {
        @apply font-lato font-light leading-6 no-underline mb-6 text-xl;
    }

    section :global(a) {
        @apply text-coral underline;
    }

    section :global(a:hover) {
        @apply font-normal;
    }

    section :global(#business-content),
    section :global(#article-content) {
        @apply text-coral;
    }
</style>
